"use client";

import { useForm } from "react-hook-form";

const onboardingForm = ({industries}) => {

    useForm()
  return (
    <div>
      Onboarding Form
    </div>
  )
}

export default onboardingForm
